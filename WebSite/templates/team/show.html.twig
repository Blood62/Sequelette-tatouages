{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
<div class="card bg-dark border border-primary mt-2  shadow rounded  ml-3 mr-3 text-white ">
    <div class="mx-auto bg-dark border border-secondary mt-3 mb-3 p-2 shadow-lg">
        <div class="card-body">
            <img id="card-img" src="{{ vich_uploader_asset(team, 'imageFile') }}" class="card-img-top" alt="picture{{ team.id }}">
            <h5 class="card-title  mt-3 "><u>Auteur:</u><span class="text-success">{{ team.name }}</span></h5>
             <p class="card-text mt-2 "><h5><u>Spécialités:</u></h5> {{ team.speciality }}</p>
            {% if app.user and app.user.roles[0] == "ROLE_ADMIN" %}
                <button type="button" id="admin" class="btn btn-outline-warning btn-lg btn-block mt-3">Administration</button>
                <div class="admin">
                    <a class="btn btn-outline-warning btn-lg btn-block mt-3 " href="{{ path('team_edit', {'id': team.id}) }}" role="button">Modifier</a>
                    {{ include('admin/team/_delete_form.html.twig') }}</div> {% endif %}
            <a class="btn btn-outline-primary btn-lg btn-block mt-3 " href="{{ path('team_index') }}" role="button">Retour à la liste</a>
        </div>
    </div>

</div>
{% endblock %}
{#<h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team.Name }}</td>
            </tr>
            <tr>
                <th>Speciality</th>
                <td>{{ team.speciality }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ team.filename }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ team.updatedAt ? team.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_index') }}">back to list</a>

    <a href="{{ path('team_edit', {'id': team.id}) }}">edit</a>

    {{ include('team/_delete_form.html.twig') }}#}