{% extends 'base.html.twig' %}

{% block title %}Corp{% endblock %}

{% block body %}
<div class="card bg-dark border border-primary mt-2  shadow rounded  ml-3 mr-3 text-white ">
    <div class="mx-auto bg-dark border border-secondary mt-3 mb-3 p-5 shadow-lg">
        <h2>partie du corp séléctionner: </h2><br>
        <h3>{{ corp.partie }}</h3>
        <button type="button" id="admin" class="btn btn-outline-warning btn-lg btn-block mt-3">Administration</button>
        <div class="admin">
            <a class="btn btn-outline-warning btn-lg btn-block mt-3 " href="{{ path('corp_edit', {'id': corp.id}) }}" role="button">Modifier</a>
            {{ include('admin/corp/_delete_form.html.twig') }}</div>
        <a class="btn btn-outline-primary btn-lg btn-block mt-3 " href="{{ path('corp_index') }}" role="button">Retour à la liste</a>

    </div>
</div>





{% endblock %}
{#    <h1>Corp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corp.id }}</td>
            </tr>
            <tr>
                <th>Partie</th>
                <td>{{ corp.partie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('corp_index') }}">back to list</a>

    <a href="{{ path('corp_edit', {'id': corp.id}) }}">edit</a>

    {{ include('corp/_delete_form.html.twig') }}#}