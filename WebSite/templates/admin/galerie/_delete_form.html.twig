<form method="post" action="{{ path('galerie_delete', {'id': galerie.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer la photo N°{{ galerie.id }} de {{ galerie.auteur }} ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ galerie.id) }}">
    <button class="btn btn-outline-danger btn-lg btn-block mt-3">Supprimer</button>
</form>
