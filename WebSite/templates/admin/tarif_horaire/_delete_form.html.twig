<form method="post" action="{{ path('tarif_horaire_delete', {'id': tarif_horaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif_horaire.id) }}">
    <button class="btn">Delete</button>
</form>
