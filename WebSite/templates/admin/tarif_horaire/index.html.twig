{% extends 'base.html.twig' %}

{% block title %}TarifHoraire index{% endblock %}

{% block body %}
    <h1>TarifHoraire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ValeurTarif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tarif_horaire in tarif_horaires %}
            <tr>
                <td>{{ tarif_horaire.id }}</td>
                <td>{{ tarif_horaire.valeurTarif }}</td>
                <td>
                    <a href="{{ path('tarif_horaire_show', {'id': tarif_horaire.id}) }}">show</a>
                    <a href="{{ path('tarif_horaire_edit', {'id': tarif_horaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tarif_horaire_new') }}">Create new</a>
{% endblock %}
